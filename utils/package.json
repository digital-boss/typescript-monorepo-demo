{
  "name": "@app/utils",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist node_modules",
    "build": "rimraf dist && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "matcher": "^5.0.0"
  }
}
